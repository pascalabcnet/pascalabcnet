begin
  var c := 'z';
  var i := 123;
  var r := 3.14;
  var s := 'PascalABC.NET';
  var a := Arr(1,2,3);
  writeln(c.GetType);
  writeln(i.GetType);
  writeln(r.GetType);
  writeln(s.GetType);
  writeln(a.GetType);
end.