var arr : array[1..10] of integer;

procedure test(t: System.Type);
begin
  
end;

begin
  //writeln(arr._upper_value - arr._lower_value);
  //test(arr.GetType);
  writeln(arr.NullBasedArray.Length);
  readln;
end.