var b : byte;
    c : char;
    i : integer;
    sm : smallint;
    sh : shortint;
    w : word;
    lw : longword;
    li : longint;
    ui : uint64;
    s : single;
    r : real;
    f : boolean;
    str : string;
    
begin
 b := Succ(b);
 c := Succ(c);
 i := Succ(i);
 sm := Succ(sm);
 sh := Succ(sh);
 w := Succ(w);
 lw := Succ(lw);
 li := Succ(li);
 //ui := Succ(ui);
 ui := ui+1;
end.