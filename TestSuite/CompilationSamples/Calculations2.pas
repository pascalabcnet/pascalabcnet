// Вывод результатов вычислений. Используются именованные константы
const 
  a = 121;
  b = 363;

begin
  writeln('Вычисления:');
  writeln;
  writeln(a,' + ',b,' = ',a+b);
  writeln(a,' - ',b,' = ',a-b);
  writeln(a,' * ',b,' = ',a*b);
  writeln(a,' / ',b,' = ',a/b);
end.