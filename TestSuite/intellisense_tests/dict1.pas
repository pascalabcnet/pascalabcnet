begin
  var Floors := new Dictionary<(longword, longword), List<integer>>;
  var sz := (1, 2);
  foreach var a{@var a: (longword,longword);@} in Floors.Keys do
  begin
    
  end;
  var l{@var l: List<integer>;@} := Floors[sz];
  l.Add(23);
 
end.